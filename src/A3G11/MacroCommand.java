package A3G11;

/**
 * @brief Abstract class representing a macro command.
 *
 * This abstract class serves as a base for creating macro commands, which are commands that can execute a sequence of commands as a single action. It implements the Command interface, providing a framework for defining composite command behaviors.
 */
public abstract class MacroCommand implements Command{
     // This class intentionally left blank to serve as a base for concrete macro command implementations.
}
